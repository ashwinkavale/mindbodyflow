# Website skills

These Markdown files are the assistant's knowledge source. Add or update files here when training-program details change. The widget loads every file listed in `src/components/AssistantWidget.tsx` before it asks Gemini for an answer.

Keep facts specific to Mind Body Flow, and avoid adding private credentials or sensitive personal information. This folder is public because the current integration calls Gemini directly from the browser.
