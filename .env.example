# Temporary UI-only Gemini integration. This key will be exposed to the browser.
# Move Gemini calls behind a server API before production use.
NEXT_PUBLIC_GEMINI_API_KEY=your_gemini_api_key_here
NEXT_PUBLIC_GEMINI_MODEL=gemini-3.6-flash
